# AgentFactory Memory

- AF-153 merge repair (2026-09-23): retain the current approved delivery across a repair submission so main's transactional merge reconciliation can finish it, including in review. Reconcile confirmed merges before reserving retries rather than excluding them forever from the queue. Match dispatcher cancellation to the actual claim, preserve stale-claim submission fencing, and rebuild core before dependent tests. Ignore local `.npm-cache/` and verify it is absent from the Git index before handoff.

- Delivery recovery clarification (2026-09-14): the user requested implementing the merged-PR retry fix in AgentFactory, not only filing AF-153. A confirmed merge of the current approved delivery should finish the original task even if its recorded checks failed; preserve CI evidence and treat further repair as separate work. This supersedes the earlier rule limiting recovery to metadata refresh outside Delivering. When task wording is ambiguous, retain the implementation request while awaiting clarification instead of substituting a backlog task.
- Merged-delivery fix implemented locally (2026-09-14): core now reconciles observation/retry/claim in a transaction; dispatcher reconciles known merges before spawn and stops completed repair process trees without worktree cleanup. Guard late submissions/review mutations and replacement PRs; explicit Done-to-Queued reopen removes the old active delivery. Preserve provider check results (including ADO pending), never fabricate passing checks. Verification: production build and all 1,388 tests across 146 files passed. Existing running services have not been restarted to load these changes.

- AF-144 delivery recovery (2026-09-14): a human can merge a PR after the watcher bounces its failed CI. Continue refreshing delivery metadata during queued/in-progress/blocked/review states, but only Delivering may auto-close or requeue; do not revoke an active repair claim. Guard slow observations against status/delivery replacement. Default worker verification must include configured CI lint, formatting and type checks, and repair handoff must reuse only an OPEN PR. Cross-package tests import built core exports: rebuild with `npx tsc -b` before checking dependent watcher/MCP behavior.
- Delivery metadata being correct in the API does not prove it is visible on the board: TaskCard/DetailPanel previously hid it outside Delivering. Keep the PR/check summary visible during repair, gate only delivery actions by status, render actual merged check results instead of unconditional "verifying", and normalize the provider's `#` prefix.

- AF-144 (2026-09-13): credential success does not prove an unattended worker can complete the Git lifecycle. Codex workspace-write on native Windows denies Git metadata writes, including FETCH_HEAD, branch/index locks and worktree registration; explicit permission-profile probes still failed on CLI 0.154.0. Pinned implementation MCP workers now use task_git for prepare/commit/push/cleanup, scoped to the active claim's repository, branch and worktree. Verify the entire lifecycle with real Git (including reclaim, unpublished/dirty cleanup refusal and junction checks); keep edits/tests sandboxed. Git hooks do not run in this broker: required verification belongs in the sandboxed worker. Fresh worker MCP processes load rebuilt dist, so this fix needs no dispatcher restart.

- GitHub worker setup: forward the resolved workspace PAT through both Git HTTP headers and GH_TOKEN for `gh pr create`. Codex's inherited `shell_environment_policy.inherit="core"` drops both; the dispatcher must allow the managed variable names into tool shells without putting secrets in argv or forwarding unrelated credentials. Verify inside `codex sandbox`, not only on the host.
- AF-143 (2026-09-13): reproduced Schannel SEC_E_NO_CREDENTIALS inside the elevated Windows sandbox. OpenSSL alone then failed because global http.sslCAInfo pointed to an Azure DevOps-only CA. LytEasy's local Git config now selects OpenSSL and Git's standard CA bundle for https://github.com/; certificate verification stays enabled. With managed shell credentials forwarded, both sandboxed `git ls-remote` and `gh api` succeed. Scope this machine-specific TLS fix to the affected checkout; do not disable SSL verification or the sandbox.

- Dispatcher stage selection must be validated and routed, not merely present in JSON: pin a worker's MCP claim to its intended task and stage so concurrent claims cannot run a different stage on the chosen model. Codex CLI exit 0 or a model-written PROBE_OK is not proof of tool success; inspect the completed MCP event/result and account for pre-claim tool denials in retry limits. Validate complete telemetry TOML, not just argument substrings. Live probes using --ignore-user-config must explicitly retain the configured Windows sandbox backend.

- Competitive assessments: include Multica (https://github.com/multica-ai/multica) alongside Vibe Kanban; verify current workflow and run-recovery documentation before claiming differentiation. Boards, remote runtimes, review handoffs, retry history, and reusable skills are overlapping capabilities, not established unique advantages.

- Delivery watcher pattern: when a git-host PR is open but cannot be completed for a host-reported reason such as merge conflicts, do not leave the task in `delivering` and do not hide it under generic CI failure. Add a specific `failure/v1` reason, bounce to `queued`, and include same-branch repair instructions so the dispatcher reclaims the persisted branch and updates the existing PR.
- Dependency waiting is claim-readiness, not a universal task state: only Backlog/Queued dependents should display "Waiting on N". If a prerequisite is reopened after its dependent starts, the unmet count may rise but must not imply that active or completed work was interrupted.
- A successful Vite production build does not typecheck the React client. When a required `Task`/`TaskDetail` field is added, run `npm -w packages/web run typecheck:client` and update every typed fixture; preserve `exactOptionalPropertyTypes` by omitting optional props instead of passing explicit `undefined`.
- When a drawer navigates between entity keys without unmounting, guard async detail responses by both the current key and a request generation. Otherwise an older request can resolve last and pair the new header with stale body/actions.
- A supervisor Restart action must match the failure's lifecycle: dispatcher failures stay `queued`, reviewer failures stay `in_review`/`delivering`, and the owning supervisor must reconcile the `restart/v1` marker by clearing both its skip-list and attempt counter. Never swallow a restart API error in the drawer.
- On Windows, killing a spawned `.cmd` wrapper does not reliably kill its CLI descendant. Supervisor timeouts/shutdown must terminate the full process tree, clear reused final-output files before spawn, and accept only a completed Codex final-message artifact at the timeout boundary (never partial streaming stdout). Re-read that artifact in the timed-out reap path because it can appear while tree termination is in progress.
- SQLite migration slots are positional, not semantic. A deployed database can report the latest `user_version` after a divergent branch consumed that number while still lacking the expected schema; verify critical tables/columns, then reserve the next migration slot for an idempotent reconciliation that preserves unknown columns.
- Re-read a task's derived failure immediately before exercising restart behavior on a live supervisor. A fresh 1/N failure may supersede the earlier skip-listed N/N failure while a review is active; that task should auto-retry and correctly reject Restart, so use a currently `skipListed: true` task for the live proof.
- In PowerShell, quote Git revision syntax such as `'@{u}'`; unquoted `@{...}` is parsed as a hashtable literal before Git receives it.
- On Windows, preserve the repository's canonical path casing for builds and checks: resolve it with `Get-Location` or `git rev-parse --show-toplevel` before briefing subagents or invoking analyzers. AgentFactory is `C:\Git\AgentFactory` (capital `G`), even when the environment supplies `C:\git\AgentFactory`.
- AF-144 follow-up (2026-09-14): Codex build environment must be supplied even without managed Git credentials; buildCodexArgs previously gated the entire Windows path allowlist on shellEnvKeys. Both omitted/empty cases now have regressions. An allowlist cannot restore values absent from the parent; current dispatcher paths were verified. NuGet initialization succeeds in a fresh Codex shell, but sandbox HTTPS still reproduces Schannel SEC_E_NO_CREDENTIALS, including TLS 1.2 and unelevated probes. Host Release build succeeded with audit/certificate verification enabled; subsequent sandbox build succeeded using that cache, which is not a permanent TLS fix. Do not restart a dispatcher without checking active claims immediately beforehand: AF-145 became active during this investigation.
- AF-145 recovery (2026-09-14): successful task_git cleanup may be followed by late tools recreating node_modules at the old path. Directory existence/nonemptiness is not proof of a worktree. prepare must inspect Git registration and the .git marker; preserve all contents of an unregistered leftover directory under a unique sibling path, recreate the persisted branch, and report the preserved path. Never relocate a registered worktree with missing metadata or accept a mismatched checkout. Regress cleanup -> late cache/unknown-file writes -> prepare with real Git. Live recovery verified: 8 focused MCP tests passed; after requeue, the fresh worker returned task_git prepare ok:true with preservedPath and resumed AF-145 in_progress. A built fix does not itself unblock an already blocked task.
- Task token breakdown: aggregate individual task_metric rows by model and recorded engine, not the latest task model. Preserve null vs zero and reconcile input/output sums. Historical stages follow the preceding agent_session timestamp; unknown attribution stays explicit, and reviewer/visualizer activity cannot be reconstructed from these records alone.
- Review model preference (2026-09-14): every new board task review uses both Claude Fable (`claude-fable-5-1`) and GPT-6 Astra (`gpt-6-astra`, medium reasoning). An ordered reviewers config must actually drive both CLIs and combine their validated findings before posting ai-review/v1; posting one clean review early can auto-advance a document stage. Root runtime configs are gitignored: update them locally and keep the tracked example synchronized. Model/effort argv tests and two-review lifecycle tests prove the setting takes effect.
- AF-147 dependency isolation: a worktree containing only node_modules/.vite silently resolves @agentfactory/core through the parent checkout's junction, producing false missing-export/runtime failures against another branch's dist. Bootstrap each npm worktree with npm ci --cache .npm-cache (ignored), then build before tests. Do not hand-create junctions or write the shared/global cache from sandboxed workers. Recheck import.meta.resolve('@agentfactory/core') from the worktree, then fix any remaining real test failures separately.
- AF-146/147/153 dual-review correction: Claude Fable legitimately used Bash to inspect the submitted branch, but --max-turns 1 ended the session before its verdict. Remove the one-turn cap; use read-only plan permissions and the existing reviewMinutes timeout. Treat CLI Error output/nonzero Claude exits as execution failures before JSON parsing. Tests that only validate model argv or fabricated final output do not prove a real tool-using review can finish; verify actual review completion after rollout. Live verification: all three completed both configured reviews and posted combined findings (9/7/5 respectively); failure flags cleared. Reviewer tests 116 passed; build passed. Fix committed locally as d8408c2.
- Review list preference (2026-09-15): show only high-priority AI findings in ReviewActions; current severity error maps to high priority. Filter before rendering and composing feedback/PR review text so hidden warning, info, or unclassified findings are never silently selected. Keep the full stored verdict and existing approval semantics. Cover mixed severity and no-high-priority cases; verify the running web app serves the rebuilt client.
- Consensus review request (2026-09-15): user wants reviewers to discuss and agree on findings, beyond concatenating independent outputs. Proposed design/plan committed as dce1677; not implemented or enabled. Keep Claude Fable + GPT-6 Astra medium, independent discovery, cross-examination, one bounded rebuttal, unanimous high-priority checklist, and explicit disputes for human decision. Never turn an empty agreed list with unresolved candidates into a clean verdict or automatic document-stage advancement.
- Consensus review implemented and enabled (2026-09-15): independent discovery, complete per-candidate cross-examination, at most one final ballot, unanimous defect/severity confirmation, and mutually agreed duplicate merging. v2 stores explicit disputes and complete discussion; disputed documents never auto-advance. Core validates the submission fingerprint inside the publication transaction; supervisor also checks immutable head/base commits and the 30-minute total deadline. Guard JSON extraction against Markdown fences embedded in serialized review history; compare normalized findings structurally, not property insertion order. Nonzero Codex exits cannot supply consensus votes. Production reviewer PID 26388 and web PID 11700 loaded compatible builds. Full suite 1446 passed plus final 8-test supervisor run; real isolated Claude Fable/Astra cycle completed four sessions and merged two reports into one confirmed finding. Evidence: logs/consensus-smoke-1789426265884/verification.json. Only new reviews use consensus; existing reviews were not rerun.
- Consensus commit 22c28f4 also verified from a detached clean checkout with its own npm ci dependencies: production build, client typecheck, and all 28 consensus tests passed. Tests that simulate changed submissions must use request-changes, claim, and submit; updateTask only permits backlog edits. Unrelated dirty files were preserved with only feature hunks staged; temporary verification worktree and isolated services were cleaned up.
- Durable retry reservations must finish the reap before polling for a replacement slot; cancelled pre-launch or transient-post reservations must refund and remove the non-attempt so its number can be reused. Preserve legacy `failure/v1` restart behavior when older comments omit `source`.
- Retry-budget tests that seed legacy failure markers must materialize the legacy budget before adding an independent operation; otherwise the compatibility seed correctly makes the first reservation unavailable.
- `node:sqlite` statement results need an explicit `as unknown as Row` cast when projecting joined retry rows under the repository's strict TypeScript build.
- When retry budgets are seeded from legacy failure markers without attempt rows, the next reservation number must fall back to the persisted `attempts_used`; active rows still derive the number from their maximum attempt.
- Stale dispatcher claims carry only a label-derived attempt after restart; persist that exact orphan attempt at the core boundary before releasing the claim, including reconciling any matching active reservation.
- Worktree tests that import workspace packages can resolve the parent repository's junctions when dependencies are absent locally; run `npm install --ignore-scripts --no-audit --no-fund` in the worktree before rebuilding package outputs.
- After reservation reconciliation exhausts the target budget, reap must not settle the refunded obsolete reservation ID.
- Keep shared delivery completion helpers state-agnostic for the existing force-complete API; enforce `pr_state = 'merged'` only at new retry/late-submit reconciliation call sites, and exclude intentional delivering/done reopens from retry reconciliation.
- Capture repair-retry intent before its write transaction so a watcher winning the race into `done` cannot be mistaken for an intentional reopen; when merged completion ends the DB session, the dispatcher must also terminate the matching live child and settle its reservation without releasing or republishing the completed task.
- Treat each claim's persisted `claimedAt` as its execution fence: carry it through the worker submit protocol and compare it inside the write transaction so a late result cannot replace a newer claim after completion and reopen.
- Dispatcher cancellation must also recognize a completed claim from durable status activity when a task was reopened before the next poll; current `claimedBy` and delivery rows may already describe only the replacement episode.

- AF-153 cache cleanup (2026-09-23): remove previously tracked .npm-cache files from the index as well as adding the ignore rule; gitignore alone cannot untrack committed files. Preserve unregistered leftover worktree directories before recreating them, and verify git rev-parse --show-toplevel before task Git operations. Verified 1,350 tests, production build and client typecheck.
