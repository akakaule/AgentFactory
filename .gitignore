node_modules/
.npm-cache/
dist/
*.tsbuildinfo
*.db
*.db-shm
*.db-wal
.DS_Store

# superpowers tooling working space (brainstorm server scratch / runtime state)
.superpowers/

# per-task agent worktrees (one task = one worktree, see packages/mcp/README.md)
.worktrees/

# local scratch scripts (machine-specific paths)
.af-*.mjs

# dispatcher + reviewer runtime: machine-specific config + per-session logs/mcp-config files
dispatcher.config.json
reviewer.config.json
intake.config.json
intake.env
start-intake.ps1
logs/

# agent / codex CLI tooling (skills + config — tooling state, not project code)
.agents/
.codex/
e2e/demo-*/
e2e/node_modules/
e2e/.demo-run.json
e2e/.env.local
